import { useDispatch, useSelector } from "react-redux";

import React from "react";

const About = () => {
  useSelector((state) => state);

  return <div>it's an app, and i made it</div>;
};

export default About;
